name: supabase_notes_app
description: "Fast Supabase Notes App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Убраны несуществующие assets и fonts
  # Если нужны кастомные шрифты или изображения, создайте соответствующие папки